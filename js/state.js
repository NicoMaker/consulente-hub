/* ==========================================================================
   STATE — stato condiviso tra i componenti.
   I contenuti veri e propri (nome, testi, aree, link) NON stanno più qui:
   sono in "data.json", nella cartella principale del progetto. Modifica
   quel file a mano per personalizzare l'hub — non serve toccare JavaScript.
   Questo file dichiara solo le variabili che app.js riempie all'avvio.
   ========================================================================== */

let SITE_CONFIG = null;
let AREAS = null;
